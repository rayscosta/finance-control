# 🏗️ Arquitetura do Sistema Finance Control

## 📊 Diagrama ER Simplificado

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         FINANCE CONTROL DATABASE                         │
│                     TimescaleDB-Optimized Architecture                   │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│    USER      │
│──────────────│
│ id (PK)      │───┐
│ email        │   │
│ password     │   │
│ name         │   │
│ resetToken   │   │
│ resetExpires │   │
└──────────────┘   │
                   │
                   ├─────────────────────────────────────────────┐
                   │                                             │
                   │                                             │
                   ▼                                             ▼
         ┌──────────────┐                            ┌──────────────┐
         │   ACCOUNT    │                            │  CATEGORY    │
         │──────────────│                            │──────────────│
         │ id (PK)      │──────┐                     │ id (PK)      │──┐
         │ userId (FK)  │      │                     │ userId (FK)  │  │
         │ name         │      │                     │ name         │  │
         │ type         │      │                     │ type         │  │
         │ initialBal   │      │                     │ isGlobal     │  │
         │ currentBal   │      │                     │ color        │  │
         │ currency     │      │                     │ icon         │  │
         │ isActive     │      │                     └──────────────┘  │
         └──────────────┘      │                                       │
                               │                                       │
                               ▼                                       │
                     ┌──────────────────┐                             │
                     │   TRANSACTION    │◄────────────────────────────┘
                     │──────────────────│  (HYPERTABLE)
                     │ id (PK)          │
                     │ userId (FK)      │
                     │ accountId (FK)   │
                     │ categoryId (FK)  │
                     │ amount           │
                     │ type             │
                     │ description      │
                     │ date ⏰          │  ← Partitioned by date
                     │ extractData      │     (1 month chunks)
                     │ isRecurring      │
                     └──────────────────┘
                               │
                               │  Continuous Aggregates:
                               │  ├─ transactions_daily
                               │  ├─ transactions_monthly_by_category
                               │  ├─ account_balances_monthly
                               │  └─ spending_weekly
                               │
                     ┌──────────────────┐
                     │   AUDIT_LOG      │  (HYPERTABLE)
                     │──────────────────│
                     │ id (PK)          │
                     │ userId (FK)      │
                     │ action           │
                     │ entity           │
                     │ entityId         │
                     │ oldValue         │
                     │ newValue         │
                     │ createdAt ⏰     │  ← Partitioned by createdAt
                     └──────────────────┘

         ┌──────────────┐
         │  CREDIT_CARD │
         │──────────────│
         │ id (PK)      │──────┐
         │ userId (FK)  │      │
         │ name         │      │
         │ bank         │      │
         │ lastDigits   │      │
         │ limit        │      │
         │ closingDay   │      │
         │ dueDay       │      │
         └──────────────┘      │
                               │
                               ▼
              ┌──────────────────────────┐
              │ CREDIT_CARD_TRANSACTION  │  (HYPERTABLE)
              │──────────────────────────│
              │ id (PK)                  │
              │ creditCardId (FK)        │
              │ billId (FK)              │
              │ amount                   │
              │ description              │
              │ date ⏰                  │  ← Partitioned by date
              │ installments             │
              │ currentInstallment       │
              └──────────────────────────┘
                               │
                               ▼
              ┌──────────────────────────┐
              │   CREDIT_CARD_BILL       │
              │──────────────────────────│
              │ id (PK)                  │
              │ creditCardId (FK)        │
              │ amount                   │
              │ dueDate                  │
              │ closingDate              │
              │ isPaid                   │
              │ paidAt                   │
              └──────────────────────────┘

         ┌──────────────┐
         │    BUDGET    │
         │──────────────│
         │ id (PK)      │
         │ userId (FK)  │
         │ categoryId   │
         │ amount       │
         │ spent        │
         │ month        │
         │ year         │
         └──────────────┘

         ┌──────────────┐
         │     GOAL     │
         │──────────────│
         │ id (PK)      │
         │ userId (FK)  │
         │ name         │
         │ targetAmount │
         │ currentAmt   │
         │ deadline     │
         └──────────────┘

         ┌──────────────────┐
         │    RECURRING     │
         │──────────────────│
         │ id (PK)          │
         │ userId (FK)      │
         │ accountId (FK)   │
         │ categoryId (FK)  │
         │ amount           │
         │ type             │
         │ description      │
         │ frequency        │
         │ startDate        │
         │ endDate          │
         │ isActive         │
         │ lastRun          │
         │ nextRun          │
         └──────────────────┘
```

## 🔄 Fluxo de Dados TimescaleDB

```
┌─────────────────────────────────────────────────────────────────┐
│                      DATA FLOW ARCHITECTURE                      │
└─────────────────────────────────────────────────────────────────┘

1. INSERT de Transação
   │
   ▼
┌──────────────────┐
│   TRANSACTION    │  (Hypertable)
│   Raw Data       │  ← Insert normal, particionamento automático
└──────────────────┘
   │
   │  Automatic Refresh (every 1 hour)
   │
   ▼
┌─────────────────────────────────────────────────────────────────┐
│              CONTINUOUS AGGREGATES (Materialized Views)          │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────┐  ┌──────────────────────────────────┐ │
│  │ transactions_daily  │  │ transactions_monthly_by_category │ │
│  │ • Daily summaries   │  │ • Monthly category totals        │ │
│  │ • Per user/account  │  │ • Budget comparisons             │ │
│  │ • Refresh: 1h       │  │ • Refresh: 1d                    │ │
│  └─────────────────────┘  └──────────────────────────────────┘ │
│                                                                  │
│  ┌─────────────────────┐  ┌──────────────────────────────────┐ │
│  │ spending_weekly     │  │ account_balances_monthly         │ │
│  │ • Weekly patterns   │  │ • Balance evolution              │ │
│  │ • Income vs expense │  │ • Per account                    │ │
│  │ • Refresh: 6h       │  │ • Refresh: 1d                    │ │
│  └─────────────────────┘  └──────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
   │
   │  Fast SELECT queries (pre-aggregated data)
   │
   ▼
┌──────────────────┐
│   DASHBOARD      │  ← Instant response (milissegundos)
│   Frontend       │  ← No need to aggregate millions of rows
└──────────────────┘

3 months later...
   │
   ▼
┌──────────────────┐
│   COMPRESSION    │  ← Automatic compression policy
│   Old chunks     │  ← 90%+ space savings
└──────────────────┘
```

## 🏛️ Arquitetura de Aplicação

```
┌─────────────────────────────────────────────────────────────────┐
│                     APPLICATION ARCHITECTURE                     │
└─────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│                          FRONTEND                               │
│  (HTML + CSS + Vanilla JavaScript + Chart.js)                  │
├────────────────────────────────────────────────────────────────┤
│  • login.html          - Authentication                         │
│  • dashboard.html      - Main dashboard with charts             │
│  • transactions.html   - CRUD transactions                      │
│  • accounts.html       - Account management                     │
│  • categories.html     - Category system (hybrid)               │
│  • import.html         - Bank statement imports                 │
│                                                                  │
│  JS Modules:                                                    │
│  • auth.js             - JWT authentication                     │
│  • api.js              - API client                             │
│  • dashboard.js        - Charts and metrics                     │
│  • transactions.js     - Transaction CRUD                       │
│  • accounts.js         - Account CRUD                           │
│  • categories.js       - Category management                    │
│  • import.js           - File upload and parsing                │
└────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP/REST API
                              │ JWT Bearer Token
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│                         BACKEND API                             │
│         (Node.js + Express + TypeScript + Prisma)              │
├────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    │
│  │   Routes     │───►│ Controllers  │───►│   Services   │    │
│  │              │    │              │    │              │    │
│  │ • auth.ts    │    │ • Auth       │    │ • Auth       │    │
│  │ • users.ts   │    │ • User       │    │ • User       │    │
│  │ • txns.ts    │    │ • Transaction│    │ • Transaction│    │
│  │ • accts.ts   │    │ • Account    │    │ • Account    │    │
│  │ • cats.ts    │    │ • Category   │    │ • Category   │    │
│  └──────────────┘    └──────────────┘    └──────────────┘    │
│                                │                               │
│                                │                               │
│  ┌──────────────┐    ┌──────────────────┐                    │
│  │  Middleware  │    │   Prisma ORM     │                    │
│  │              │    │                  │                    │
│  │ • Auth       │    │ • Type-safe      │                    │
│  │ • Validation │    │ • Migrations     │                    │
│  │ • Error      │    │ • Relations      │                    │
│  │ • Logger     │    │ • Transactions   │                    │
│  └──────────────┘    └──────────────────┘                    │
│                                │                               │
└────────────────────────────────│───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│                    DATABASE LAYER                               │
│              (PostgreSQL 16 + TimescaleDB 2.x)                 │
├────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Regular Tables:           Hypertables (Time-Series):          │
│  ├─ users                  ├─ transactions ⏰                  │
│  ├─ accounts               ├─ credit_card_transactions ⏰      │
│  ├─ categories             └─ audit_logs ⏰                    │
│  ├─ credit_cards                                               │
│  ├─ credit_card_bills      Continuous Aggregates:              │
│  ├─ budgets                ├─ transactions_daily               │
│  ├─ goals                  ├─ transactions_monthly_by_category │
│  └─ recurring_transactions ├─ account_balances_monthly         │
│                             └─ spending_weekly                  │
│                                                                  │
│  Features:                                                      │
│  • Automatic partitioning (1 month chunks)                     │
│  • Compression policies (data > 3 months)                      │
│  • Continuous aggregate refresh (hourly/daily)                 │
│  • Timezone-aware timestamps (Timestamptz)                     │
│  • JsonB for semi-structured data                              │
│  • 25+ optimized indexes                                       │
└────────────────────────────────────────────────────────────────┘
```

## 🚀 Performance Optimizations

### **Query Performance**

```
┌─────────────────────────────────────────────────────────────────┐
│                    QUERY OPTIMIZATION STRATEGY                   │
└─────────────────────────────────────────────────────────────────┘

Without TimescaleDB:
   Query: "Show daily spending for last 30 days"
   ↓
   Scan entire transactions table (millions of rows)
   ↓
   Group by day
   ↓
   Calculate SUM for each day
   ↓
   Response time: 2-5 seconds ❌

With TimescaleDB:
   Query: "Show daily spending for last 30 days"
   ↓
   SELECT from transactions_daily (pre-aggregated!)
   ↓
   WHERE bucket >= NOW() - INTERVAL '30 days'
   ↓
   Response time: 10-50ms ✅
   
   Performance gain: 40-500x faster! 🚀
```

### **Storage Optimization**

```
┌─────────────────────────────────────────────────────────────────┐
│                    STORAGE COMPRESSION                           │
└─────────────────────────────────────────────────────────────────┘

Uncompressed:
   1 million transactions × 500 bytes = 500 MB
   5 years of data = 2.5 GB

Compressed (TimescaleDB):
   Compressed chunks (data > 3 months)
   Compression ratio: 10:1 typical
   5 years of data = 250 MB
   
   Storage savings: 90% less disk space! 💾
```

## 📈 Scalability Features

### **Data Growth Handling**

```
Year 1:   1K users × 100 txns/month = 1.2M txns/year
Year 2:  10K users × 100 txns/month = 12M txns/year
Year 3: 100K users × 100 txns/month = 120M txns/year

With TimescaleDB:
✅ Automatic partitioning prevents table bloat
✅ Compression keeps disk usage linear
✅ Continuous aggregates ensure dashboard stays fast
✅ Queries remain under 100ms regardless of data size
```

## 🔒 Security & Compliance

```
┌─────────────────────────────────────────────────────────────────┐
│                    AUDIT & COMPLIANCE                            │
└─────────────────────────────────────────────────────────────────┘

Every critical operation is logged:
   ↓
audit_logs (hypertable)
   ↓
Immutable log (append-only)
   ↓
Compressed after 6 months
   ↓
Retention: 5-7 years (configurable)

Features:
• Who did what, when, where (IP, user agent)
• Before/after values (JsonB)
• Fast queries with indexes
• Compressed storage for compliance
```

---

**Esta arquitetura está pronta para:**
- 📈 Escalar para milhões de usuários
- 🚀 Manter performance consistente
- 💾 Minimizar custos de armazenamento
- 🔒 Atender requisitos de compliance
- 📊 Fornecer analytics em tempo real
